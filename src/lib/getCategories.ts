import categories from '../data/categories.json'

/* type Category = {
    title: string,
    icon: string,
    color: string,
} */

export default function getCategories() { 
    return categories
}
